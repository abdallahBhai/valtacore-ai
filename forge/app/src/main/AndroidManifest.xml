<?xml version="1.0" encoding="utf-8"?>
<!--
  The permissions gauntlet (declared, requested progressively in onboarding):
  - PACKAGE_USAGE_STATS: the engine's primary signal (special access screen).
  - SYSTEM_ALERT_WINDOW: Checkpoint overlay.
  - BIND_ACCESSIBILITY_SERVICE: local domain-list matching for Checkpoint.
  - ACTIVITY_RECOGNITION: steps.
  - ACCESS_FINE_LOCATION: GPS gym check-ins (foreground only, geofence compare on-device).
  - CAMERA: CV-verified workouts; frames never leave the device.

  Play Store: requires the digital-wellbeing core-purpose declaration.
  Budget one rejection-and-resubmit cycle.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Network access exists ONLY for the stateless LLM calls (intent router,
         goal decomposition, Tribunal phrasing). All scoring is on-device. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ForgeApp"
        android:label="FORGE"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.CheckpointOverlayService"
            android:exported="false" />

        <service
            android:name=".service.ForgeAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver android:name=".collect.UnlockReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
